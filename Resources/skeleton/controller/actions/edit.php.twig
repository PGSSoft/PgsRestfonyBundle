
    /**
{% block phpdoc_method_header %}
     * Get a {{ entity }} edit form
     *
{% block documentation %}
{% if document %}
     * @ApiDoc(
     *   description = "Get a {{ entity }} edit form.",
     *   requirements={
     *     {
     *        "name"="{{ entity_identifier }}",
     *        "dataType"="{{ entity_identifier_type }}",
     *        "requirement"="{{ requirement_regex }}",
     *        "description"="{{ entity }} identifier."
     *     }
     *   },
     *   output="{{ namespace }}\Form\{{ entity_namespace }}{{ entity }}Type",
     *   statusCodes={
     *     200 = "{{ entity }} Form type."
     *   }
     * )
     *
{% endif %}
{% endblock documentation %}
     * @View(serializerEnableMaxDepthChecks=true)
     *
     * @param Request $request
     * @param $id
     *
     * @return Response
     */
{% endblock phpdoc_method_header %}
{% block method_definition %}
    public function editAction(Request $request, $id)
{% endblock method_definition %}
    {
{% block method_body %}
        return parent::editForm($request, $id);
{% endblock method_body %}
{% block method_return '' %}
    }
{% block form '' %}
